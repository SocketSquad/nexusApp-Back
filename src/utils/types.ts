export enum AttachmentType {
  IMAGE = 'image',
  FILE = 'file',
}

export enum AttachmentStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
