// Attachment types

export enum AttachmentType {
  IMAGE = 'image',
  FILE = 'file',
}

// Message types

export enum MessageType {
  TEXT = 'text',
  IMAGE = 'image',
  FILE = 'file',
}

// Friend types

export enum FriendStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  BLOCKED = 'blocked',
}
